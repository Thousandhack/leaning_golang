go 语言特性
2.天然并发
a.从语言层面支持并发,非常简单
b.goroute,轻量级线程，创建成千上完个goroute成为可能
c.基于CSP (Communicating Sequential Process) 模型实现



# go 语言的运行方式
1.
go build 
# 运算上面一句命令以后会出现一个.exe的文件
然后直接运行.exe的文件即可

2.
go build -o test.exe
# 运行上面的命令会生成test.exe的文件
然后直接运行test.exe的文件即可

3.
go install 
# 运行后会在bin目录下生成一个以 项目名.exe 的文件


还有一些问题没有解决

VSCode配置Go语言开发环境小技巧:

# vs code 配置代码段
ctrl+shift+p
让后输入>snippet
然后输入 go
这个时候会打开一个文件
go.json文件
让后在字典的里面输入：
	"print":{
		"prefix": "pln",
		"body": "fmt.Println($0)",
		"description": "打印行..."
	}

这样就可以生效，在需要输入打印的时候输入pln就会变成fmt.Println()


# SDK就是软件开发工具包的意思
# 对于GO语言开发需要安装和配置好sdk

